<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Reset Password | <%= process.env.APP_NAME || "Task MGR" %>
  </title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.css" />
</head>

<body style="background-color:#130146;">
  <% if(!user){ %>
    <div class="container">
      <div class="card mt-5">
        <div class="card-body">
          <h5 class="card-title p-2">
            <i class="fa fa-exclamation-triangle text-danger" aria-hidden="true"></i> Warning
          </h5>
          <div class="p-2 border border-danger rounded">
            <p class="card-text">Sorry!! This link is expired.</p>
          </div>
        </div>
      </div>
    </div>
    <% }else{ %>
      <div class="row mt-4">
        <div class="col-md-3"></div>
        <div class="col-md-6">
          <div class="card">
            <div class="card-header">
              <h4>Reset Password</h4>
            </div>
            <div class="card-body">
              <form class="customeForm"
                action="<%= process.env.APP_URL %>/forget-password/<%= user.forgotPasswordCode %>" method="post"
                enctype="application/json">
                <div class=" mb-3">
                  <label for="email" class="form-label">Password</label>
                  <input type="password" class="form-control" id="email" name="password">
                </div>
                <div class="mb-3">
                  <label for="confirmEmail" class="form-label">Confirm Password</label>
                  <input type="password" class="form-control" id="confirmEmail" name="confirmPassword">
                </div>
                <div class="3">
                  <button type="submit" class="btn" style="background-color:#130146;color:#ffffff">Submit</button>
                </div>
              </form>
            </div>
          </div>
        </div>
        <div class="col-md-3"></div>
      </div>
      <script src="<%= process.env.APP_URL  %>/assets/js/jquery.3.2.1.min.js"></script>
      <script src="<%= process.env.APP_URL  %>/assets/js/formSubmit.js"></script>
      <% } %>
</body>

</html>